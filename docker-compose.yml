# Local one-command bring-up:  docker compose up --build
# The app (JSON API + server-rendered UI) listens on :8000.
services:
  api:
    build: .
    image: exec-email-copilot
    ports:
      - "8000:8000"
    environment:
      LOG_LEVEL: INFO
      # Opt-in security / LLM (see .env.example):
      # API_AUTH_TOKEN: change-me
      # RATE_LIMIT_PER_MINUTE: "120"
      # OPENAI_API_KEY: sk-...
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import httpx; httpx.get('http://localhost:8000/health', timeout=5).raise_for_status()"
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
