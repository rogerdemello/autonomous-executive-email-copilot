"""Research: the deterministic RL-style benchmark this project started as.

Kept intact and reproducible, but separated from the product in ``app/`` so the
two stories stay legible. Dependencies point one way only: research may import
from ``app``, never the reverse.
"""
