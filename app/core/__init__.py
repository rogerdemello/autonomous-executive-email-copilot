"""Shared foundations: configuration, persistence, domain models, security.

Everything here is used by both the product (``app.copilot``, ``app.saas``,
``app.web``) and the research benchmark (``research.sim``), so it deliberately
depends on neither.
"""
